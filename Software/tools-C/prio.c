/*        prio utility        versie 1.2        30-5-'84        Jaron Borensztajn        prio maakt de prioriteit van het commando dat erachter        staat lager ( of hoger: alleen voor system ). Pas op        met hele hoge of hele lage prioriteiten.        gebruik: ++ prio n commando        n van 0 tot  25 :  lagere prioriteit.        n van 0 tot -25 :  hogere prioriteit. ( alleen bij system )*/#include <stdio.h>main(argc,argv)int argc;char **argv;{int pr;char *shell = "shell";char *sharg = "+bcx";char args[2000];if( argc<3 || (pr=atoi(argv[1])) == 0 && strcmp(argv[1],"0") != 0 || pr <-25 || pr >25 ) {        fprintf(stderr,"usage: ++ prio n command\n");        exit(0);        };nice(pr);argv    += 2;argc    -= 2;strcpy(args,"");while(argc-- >0) {        strcat(args,argv[0]);        strcat(args," ");        argv++;        };execl("/bin/shell",shell,sharg,args,0);}