#include <stdio.h>#include <net.h>char buffer[512];#define MYSERVER "0.0.0.0"int main(argc, argv)int argc;char **argv;{    int sfdn, i=0, count =0, clientlen;    struct sockaddr_in server;    struct sockaddr_in client;    if (argc < 2)    {       fprintf (stderr, "usage: w2server <port>\n");       exit (1);    }    sfdn = socket(AF_INET, SOCK_STREAM,0);    bzero(&server, sizeof(struct server));    server.sin_family = AF_INET;    server.sin_port = atoi (argv[1]);    server.sin_addr = (uint32_t) inet_addr(MYSERVER) ;    i = bind(sfdn,(struct sockaddrin*) &server, sizeof(server));    if (i == -1)    {        printf ("Bind error!\n");        exit(1);    }    listen(sfdn,1);    accept(sfdn, (struct sockaddr_in*)&client, &clientlen);    while(1)        {                if((count = read (0, buffer, 512)) == 0)                {                        close(sfdn);                        close (0);  /* eof */                        exit(0);                }                write (sfdn, buffer, count);         }}